pandas>=2.0
numpy>=1.24
pyyaml>=6.0
duckdb>=0.9        # out-of-core engine for the full-scale join (engine: duckdb)
# optional:
pyarrow>=12.0      # parquet read/write (Open Targets dumps, fast output)
requests>=2.28     # live STRING API network expansion (network.source: api)
